guard :shell do
  watch(/(Makefile|(.*\.cpp)|(.*\.h))/) do |m|
    title = 'Lint'
    msg = `make lint`
    status = ($?.success? && :success) || :failed

    n msg, title, status
    "-> #{msg}"
  end
end
